# App env

- `NEXT_PUBLIC_API_URL` — backend base (default `http://localhost:3001/api/v1`)
- `NEXT_PUBLIC_USE_MOCK` — `true` forces mock plan data
- `NEXT_PUBLIC_SUBSCRIPTION_CONTRACT_ID` — Sorobill contract
